#pragma once

#include <QIcon>

namespace AppIcon {

/// Creates the multi-resolution DailyReport application icon without relying
/// on an external image plugin.
QIcon create();

} // namespace AppIcon
